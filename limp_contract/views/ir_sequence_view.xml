<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="sequence_view_add_delegation_field" model="ir.ui.view">
        <field name="name">ir.sequence.form.add_delegation_field</field>
        <field name="model">ir.sequence</field>
        <field name="inherit_id" ref="base.sequence_view"/>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <field name="company_id" position="before">
                <field name="delegation_id"/>
            </field>
        </field>
    </record>

    <record id="sequence_view_tree_add_delegation_field" model="ir.ui.view">
        <field name="name">ir.sequence.tree.add_delegation_field</field>
        <field name="model">ir.sequence</field>
        <field name="inherit_id" ref="base.sequence_view_tree"/>
        <field name="type">tree</field>
        <field name="arch" type="xml">
            <field name="company_id" position="before">
                <field name="delegation_id"/>
            </field>
        </field>
    </record>
</odoo>
