<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>

        <!--record id="view_res_partner_address_filter_add_more_filters" model="ir.ui.view">
            <field name="name">res.partner.address.select</field>
            <field name="inherit_id" ref="base.view_res_partner_address_filter"/>
            <field name="model">res.partner.address</field> MIGRACION: Comentado
            <field name="type">search</field>
            <field name="arch" type="xml">
                <field name="country_id" position="after">
                    <field name="state_id"/>
                    <field name="city"/>
                </field>
                <filter string="Country" position="after">
                    <filter string="State" icon="terp-go-home" domain="[]" context="{'group_by' : 'state_id'}" />
                    <filter string="Poblation" icon="terp-go-home" domain="[]" context="{'group_by' : 'city'}" />
                </filter>
            </field>
        </record>

        <record id="view_partner_address_tree_add_state_field" model="ir.ui.view">
            <field name="name">res.partner.address.tree.add_state_field</field>
            <field name="model">res.partner.address</field>
            <field name="inherit_id" ref="base.view_partner_address_tree"/>
            <field name="type">tree</field>
            <field name="arch" type="xml">
                <field name="city" position="before">
                    <field name="state_id"/>
                </field>
            </field>
        </record-->

    </data>
</openerp>
